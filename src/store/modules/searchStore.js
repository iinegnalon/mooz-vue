const state = () => ({
  query: '',
  results: [],
  totalResults: 0,
  currentPage: 1,
  pageSize: 8,
  loading: false,
  error: null,
});

const getters = {};

const mutations = {};

const actions = {};

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions,
};
